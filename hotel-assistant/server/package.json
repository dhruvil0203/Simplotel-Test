{
  "name": "hotel-assistant-server",
  "version": "1.0.0",
  "description": "AI-Powered Hotel Guest Assistant — Express backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --verbose --forceExit"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "chrono-node": "^2.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
